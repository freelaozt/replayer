# Uncomment the next line to define a global platform for your project
# platform :macos, '12.0'

target 'rePlayer-macos' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for rePlayer-macos
  # Pods for VLC
  pod 'VLCKit', '3.3.0'
  # 添加的依赖
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SQLite.swift', '~> 0.14.1'
  pod 'GCDWebServer', '~> 3.5.4'
end
